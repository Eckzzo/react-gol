import { Root } from "@radix-ui/react-label";
import { styled } from "styles/stitches";

export const StyledLabel = styled(Root, {
	fontSize: "1.2rem",
	marginBottom: "0.4rem",
	fontWeight: "500",
	color: "$grass12",
	userSelect: "none",
});
